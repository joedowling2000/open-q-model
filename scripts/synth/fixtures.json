[
 {
  "description": "Given a list of integers arr, return true if it can be partitioned into three contiguous non-empty parts with equal sums. Constraints: 3 <= length of arr <= 20, -50 <= arr[i] <= 50.",
  "python": "def solve(arr):\n    total = sum(arr)\n    if total % 3 != 0:\n        return False\n    target = total // 3\n    parts = 0\n    running = 0\n    for i, x in enumerate(arr):\n        running += x\n        if running == target and parts < 2 and i < len(arr) - 1:\n            parts += 1\n            running = 0\n    return parts == 2 and running == target",
  "generator": "def gen_input(rng):\n    # Half the cases are constructed to BE partitionable, so the inputs can\n    # distinguish a correct solution from one that always answers false.\n    if rng.random() < 0.5:\n        part = lambda: [rng.randint(-9, 9) for _ in range(rng.randint(1, 3))]\n        a, b, c = part(), part(), part()\n        target = sum(a)\n        b = b + [target - sum(b)]\n        c = c + [target - sum(c)]\n        return {'arr': a + b + c}\n    n = rng.randint(3, 12)\n    return {'arr': [rng.randint(-50, 50) for _ in range(n)]}",
  "q_candidates": [
   "solve:{[arr]\n   total_sum:sum arr;\n   if[total_sum<>0; :0b];\n   target:total_sum%3;\n   count_parts:0; current_sum:0; i:0;\n   while[i<count arr;\n       current_sum+:arr[i];\n       if[current_sum=target; count_parts+:1; current_sum:0; if[count_parts=3; :1b]];\n       i+:1];\n   :0b}",
   "solve:{[arr]\n   t:sum arr;\n   if[0<>t mod 3; :0b];\n   target:t div 3;\n   parts:0; run:0; n:count arr;\n   i:0;\n   while[i<n;\n     run+:arr i;\n     if[(run=target) and (parts<2) and i<n-1; parts+:1; run:0];\n     i+:1];\n   :(parts=2) and run=target}"
  ]
 },
 {
  "description": "Given a list of integers nums, return the running maximum: a list where element i is the largest value among nums[0..i]. Constraints: 1 <= length of nums <= 30, -100 <= nums[i] <= 100.",
  "python": "def solve(nums):\n    out = []\n    best = None\n    for x in nums:\n        best = x if best is None else max(best, x)\n        out.append(best)\n    return out",
  "generator": "def gen_input(rng):\n    n = rng.randint(1, 30)\n    return {'nums': [rng.randint(-100, 100) for _ in range(n)]}",
  "q_candidates": [
   "solve:{[nums] maxs nums}",
   "solve:{[nums] max nums}"
  ]
 },
 {
  "description": "Given a string s of lowercase letters, return the number of distinct characters it contains. Constraints: 1 <= length of s <= 40.",
  "python": "def solve(s):\n    return len(set(s))",
  "generator": "def gen_input(rng):\n    n = rng.randint(1, 40)\n    return {'s': ''.join(rng.choice('abcdefghij') for _ in range(n))}",
  "q_candidates": [
   "solve:{[s] count distinct s}"
  ]
 },
 {
  "description": "Given a list of floats prices and an integer window, return the simple moving average over that window, as a list of the same length where the first window-1 entries use however many values are available. Constraints: 1 <= window <= 5, 1 <= length of prices <= 20.",
  "python": "def solve(prices, window):\n    out = []\n    for i in range(len(prices)):\n        lo = max(0, i - window + 1)\n        chunk = prices[lo:i+1]\n        out.append(sum(chunk) / len(chunk))\n    return out",
  "generator": "def gen_input(rng):\n    n = rng.randint(1, 20)\n    return {'prices': [round(rng.uniform(-10, 10), 3) for _ in range(n)], 'window': rng.randint(1, 5)}",
  "q_candidates": [
   "solve:{[prices;window] mavg[window;prices]}",
   "solve:{[prices;window] window mavg prices}"
  ]
 }
]